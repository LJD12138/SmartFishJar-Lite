# 智能鱼缸Lite — 软件移植修改计划

> 生成日期：2026-04-27  
> 硬件来源：SCH_Schematic1_2026-04-27.pdf / Netlist_Schematic1_2026-04-27.tel  
> 目标工程：APP（GD32F30x / FreeRTOS）

---

## IO 映射对照表

### ADC 通道（共12路，原8路需扩展）

| 信号名 | MCU引脚 | ADC通道 | 说明 |
|--------|---------|---------|------|
| IO_VIN_CURR_AD | PA0 | ADC_CH0 | 输入电流 |
| IO_VIN_VOLT_AD | PA1 | ADC_CH1 | 输入电压 |
| IO_5V_NTC_AD | PA2 | ADC_CH2 | 5V板载温度 |
| IO_WATER_NTC2_AD | PA6 | ADC_CH6 | 水温探头2 |
| IO_WATER_NTC1_AD | PA7 | ADC_CH7 | 水温探头1 |
| IO_HEAT_CURR_AD | PB0 | ADC_CH8 | 加热棒电流 |
| IO_O2_CURR_AD | PB1 | ADC_CH9 | 氧气泵电流 |
| IO_LIGHT_CURR_AD | PC0 | ADC_CH10 | 灯光总电流 |
| IO_12V_NTC_AD | PC1 | ADC_CH11 | 12V板载温度 |
| IO_12V_VOLT_AD | PC3 | ADC_CH13 | 12V输出电压 |
| IO_LIGHT_RES_AD | PC4 | ADC_CH14 | 光照传感器 |
| IO_PUMP_CURR_AD | PC5 | ADC_CH15 | 水泵电流 |

### PWM 输出

| 信号名 | MCU引脚 | 定时器/通道 | 说明 |
|--------|---------|-----------|------|
| IO_HEAT_PWM | PC6 | TIMER2_CH0（全重映射）| 加热棒 |
| IO_O2_PWM | PC7 | TIMER2_CH1（全重映射）| 氧气泵 |
| IO_FAN_PWM2 | PC8 | TIMER2_CH2（全重映射）| 风扇2 |
| IO_FAN_PWM1 | PC9 | TIMER2_CH3（全重映射）| 风扇1 |
| IO_PUMP_PWM | PA8 | TIMER0_CH0 | 水泵 |
| IO_LIGHT_PWM | PB6 | TIMER3_CH0 | 暖白光 |
| IO_LIGHT_B_PWM | PB7 | TIMER3_CH1 | 蓝光 |
| IO_LIGHT_G_PWM | PB8 | TIMER3_CH2 | 绿光 |
| IO_LIGHT_R_PWM | PB9 | TIMER3_CH3 | 红光 |
| IO_BUZ_PWM | PB10 | TIMER1_CH2（部分重映射）| 蜂鸣器 |

### GPIO 数字IO

| 信号名 | MCU引脚 | 方向 | 说明 |
|--------|---------|------|------|
| IO_12V_EN | PC2 | 输出 | 12V使能 |
| IO_LED1 | PB11 | 输出 | 状态指示灯1 |
| IO_KEY_ENTER | PC13 | 输入 | 确认键 |
| IO_KEY_LEFT | PC14 | 输入 | 左键 |
| IO_KEY_UP | PC15 | 输入 | 上键 |
| IO_KEY_DOWN | PC12 | 输入 | 下键 |
| IO_KEY_RIGHT | PD2 | 输入 | 右键 |
| IO_OLED_NSS | PB12 | 输出 | OLED片选 |
| IO_OLED_SCK | PB13 | 输出 | OLED时钟 |
| IO_OLED_MOSI | PB15 | 输出 | OLED数据 |
| IO_OLED_RES | PA11 | 输出 | OLED复位 |
| IO_OLED_DC | PA12 | 输出 | OLED命令/数据 |
| IO_PRINT_TX | PA9 | 输出 | 调试串口TX（USART0）|
| IO_PRINT_RX | PA10 | 输入 | 调试串口RX（USART0）|

---

## 任务清单

---

### 任务 1 — `board_config.h` 配置更新

**类型：修改**

```c
// ① 更新版本号
boardSOFTWARE_VERSION → "Lite_Ver_1_0_0"
boardHARDWARE_VERSION → "Lite_Ver_1_0_0"

// ② 禁用旧电源模块（确认以下保持为0）
boardDC_EN  = 0
boardUSB_EN = 0

// ③ 修改调试串口为 USART0（PA9/PA10）
boardPRINT_IFACE = 1

// ④ 新增模块开关宏（添加到"模块选择"区域）
#define boardFAN2_EN          1   // 第二路风扇
#define boardO2PUMP_EN        1   // 氧气泵
#define boardWATER_PUMP_EN    1   // 水泵
#define boardLIGHT_WARM_EN    1   // 暖白光
#define boardLIGHT_SENSOR_EN  1   // 光照传感器
#define boardWATER_TEMP_EN    1   // 外接水温NTC
#define board12V_EN           1   // 12V电源使能控制
```

---

### 任务 2 — `gpio_init.c / gpio_init.h` GPIO初始化全面更新

**类型：修改**

```
① 删除旧项目 DC/USB 电源相关GPIO初始化

② 新增 IO_12V_EN 输出初始化（PC2，默认低电平）

③ 按键更新为新引脚
   IO_KEY_ENTER → PC13
   IO_KEY_LEFT  → PC14
   IO_KEY_UP    → PC15
   IO_KEY_DOWN  → PC12
   IO_KEY_RIGHT → PD2

④ OLED SPI GPIO 更新
   IO_OLED_NSS  → PB12
   IO_OLED_SCK  → PB13
   IO_OLED_MOSI → PB15
   IO_OLED_RES  → PA11
   IO_OLED_DC   → PA12

⑤ ADC引脚更新为模拟输入模式（AIN）
   新增：PA0, PA1, PA2, PA6, PA7,
         PB0, PB1, PC0, PC1, PC3, PC4, PC5
   删除：旧DC/USB相关ADC引脚

⑥ TIMER2 全重映射使能（PC6/PC7/PC8/PC9 → AF_PP）
   gpio_pin_remap_config(GPIO_TIMER2_FULL_REMAP, ENABLE);

⑦ TIMER3 初始化（PB6/PB7/PB8/PB9 → AF_PP）

⑧ TIMER0 PA8 AF_PP（水泵PWM）

⑨ TIMER1 部分重映射，PB10 AF_PP（蜂鸣器，检查与旧配置是否冲突）
```

---

### 任务 3 — `Adc/adc_iface.h` ADC通道重新分配

**类型：修改**

```c
// 修改通道总数
#define ADC_CHANNEL_NUM    12   // 原来是 8

// 删除以下旧通道定义：
// adcDC_TEMP_*   旧DC温度
// adcDC_CURR_*   旧DC电流
// adcDC_VOLT_*   旧DC电压
// adcUSB_TEMP_*  旧USB温度
// adcUSB_CURR_*  旧USB电流
// adcUSB_VOLT_*  旧USB电压
// adcKEY_POWER_* 旧电源按键ADC
// adcSYS_IN_VOLT_* 旧系统输入电压

// 新增以下12路通道定义（格式参照现有风格）：
// adcVIN_CURR    PA0  ADC_CH0   输入电流
// adcVIN_VOLT    PA1  ADC_CH1   输入电压
// adc5V_NTC      PA2  ADC_CH2   5V板载温度
// adcWATER_NTC2  PA6  ADC_CH6   水温探头2
// adcWATER_NTC1  PA7  ADC_CH7   水温探头1
// adcHEAT_CURR   PB0  ADC_CH8   加热棒电流
// adcO2_CURR     PB1  ADC_CH9   氧气泵电流
// adcLIGHT_CURR  PC0  ADC_CH10  灯光总电流
// adc12V_NTC     PC1  ADC_CH11  12V板载温度
// adc12V_VOLT    PC3  ADC_CH13  12V输出电压
// adcLIGHT_RES   PC4  ADC_CH14  光照传感器
// adcPUMP_CURR   PC5  ADC_CH15  水泵电流
```

> ?? **注意**：DMA采样顺序必须和通道号顺序严格对应，同步更新 `vAdc_Init()` 中的 `adc_regular_channel_config()` 调用序列。

---

### 任务 4 — `Adc/adc_task.c` ADC滤波器与采样逻辑更新

**类型：修改**

```
① 删除旧的 DC/USB 相关滤波器定义和处理逻辑

② 新增12路滤波器（参照现有 FilterHandler_T 风格）：
   - VIN电压滤波器    （6点均值）
   - VIN电流滤波器    （12点均值）
   - 12V电压滤波器    （6点均值）
   - 12V_NTC滤波器    （6点均值）
   - 5V_NTC滤波器     （6点均值）
   - 加热棒电流滤波器  （12点均值）
   - 氧气泵电流滤波器  （12点均值）
   - 水泵电流滤波器    （12点均值）
   - 灯光电流滤波器    （12点均值）
   - 光照传感器滤波器  （6点均值）
   - 水温NTC1滤波器   （6点均值）
   - 水温NTC2滤波器   （6点均值）

③ 更新 AdcSamp_T 结构体成员（在 adc_task.h 中），
   替换旧成员为新的12路数据成员

④ 更新物理量换算逻辑
   - VIN电压：分压比按原理图 R13(68K)/R14(12K) 计算
   - 12V电压：分压比按原理图 R37/R38 计算
   - 电流：采样电阻 R84 按原理图值换算
   - NTC温度：沿用现有 ntc.c 的查表/公式实现
```

---

### 任务 5 — `MD_HeatManage/md_hm_iface.h` 热管理接口更新

**类型：修改（双风扇 + 新加热棒引脚）**

```c
// FAN1（新引脚）
#define fanPWM1_GPIO_RCU    RCU_GPIOC
#define fanPWM1_GPIO_PORT   GPIOC
#define fanPWM1_PIN         GPIO_PIN_9      // PC9 = TIMER2_CH3(全重映射)
#define fanTIMER            TIMER2
#define fanTIMER_RCU        RCU_TIMER2
#define fanTIMER_CH1        TIMER_CH_3

// FAN2（新增，boardFAN2_EN 保护）
#if(boardFAN2_EN)
#define fanPWM2_GPIO_RCU    RCU_GPIOC
#define fanPWM2_GPIO_PORT   GPIOC
#define fanPWM2_PIN         GPIO_PIN_8      // PC8 = TIMER2_CH2(全重映射)
#define fanTIMER_CH2        TIMER_CH_2
#endif

// 加热棒PWM（新，统一入TIMER2管理）
#define heatPWM_GPIO_RCU    RCU_GPIOC
#define heatPWM_GPIO_PORT   GPIOC
#define heatPWM_PIN         GPIO_PIN_6      // PC6 = TIMER2_CH0(全重映射)
#define heatTIMER_CH        TIMER_CH_0

// 删除：旧 fanPWM_EN GPIO（新版无单独使能引脚）
```

---

### 任务 6 — `MD_HeatManage/md_hm_task.c` 热管理任务更新

**类型：修改**

```
① vFan_PwmInit() 增加 FAN2 通道初始化

② v_fan_pwm_set() 增加 fan_id 参数，支持双路独立调速
   void v_fan_pwm_set(u8 fan_id, u16 level)

③ 加热棒PWM控制统一纳入此任务管理
   （TIMER2同时管理 HEAT/O2/FAN1/FAN2，TIMER2 Init 在此处唯一初始化）

④ 加热控制逻辑：以水温NTC1/NTC2 ADC 反馈为输入，实现分档或PID控制
```

---

### 任务 7 — `MD_Light/md_light_iface.h` 照明模块单路→四路升级

**类型：修改（改动较大）**

```c
// 原来：单路照明 TIMER1_CH1 PA1
// 新版：RGBW四路 统一使用 TIMER3

// 暖白光（boardLIGHT_WARM_EN）
#define lightW_GPIO_RCU     RCU_GPIOB
#define lightW_GPIO_PORT    GPIOB
#define lightW_PIN          GPIO_PIN_6      // PB6 = TIMER3_CH0
#define lightW_TIMER_CH     TIMER_CH_0

// 蓝光
#define lightB_GPIO_RCU     RCU_GPIOB
#define lightB_GPIO_PORT    GPIOB
#define lightB_PIN          GPIO_PIN_7      // PB7 = TIMER3_CH1
#define lightB_TIMER_CH     TIMER_CH_1

// 绿光
#define lightG_GPIO_RCU     RCU_GPIOB
#define lightG_GPIO_PORT    GPIOB
#define lightG_PIN          GPIO_PIN_8      // PB8 = TIMER3_CH2
#define lightG_TIMER_CH     TIMER_CH_2

// 红光
#define lightR_GPIO_RCU     RCU_GPIOB
#define lightR_GPIO_PORT    GPIOB
#define lightR_PIN          GPIO_PIN_9      // PB9 = TIMER3_CH3
#define lightR_TIMER_CH     TIMER_CH_3

// 公共
#define lightTIMER          TIMER3
#define lightTIMER_RCU      RCU_TIMER3
#define lightPWM_MAX_VALUE  1000

// 四路独立PWM设置宏
#define lightW_PWM_SET(x)   TIMER_CH0CV(lightTIMER) = (uint32_t)(x)
#define lightB_PWM_SET(x)   TIMER_CH1CV(lightTIMER) = (uint32_t)(x)
#define lightG_PWM_SET(x)   TIMER_CH2CV(lightTIMER) = (uint32_t)(x)
#define lightR_PWM_SET(x)   TIMER_CH3CV(lightTIMER) = (uint32_t)(x)
```

---

### 任务 8 — `MD_Light/md_light_task.c` 照明任务更新

**类型：修改**

```
① vLight_IfaceInit() 初始化 TIMER3 四个通道

② 更新光照控制结构体，增加 r / g / b / warm 四个独立分量

③ 新增自动调光功能（boardLIGHT_SENSOR_EN）
   读取 IO_LIGHT_RES_AD（光照传感器ADC），根据环境光强自动调节LED亮度

④ 更新灯光过流保护逻辑
   读取 IO_LIGHT_CURR_AD，超阈值则按比例降低亮度或关闭，保护恒流驱动芯片
```

---

### 任务 9 — **新增** `Hardware/Pump/` 水泵模块

**类型：新增**

**新建文件：**
- `Hardware/Pump/pump_iface.h`
- `Hardware/Pump/pump_task.h`
- `Hardware/Pump/pump_task.c`

```c
// pump_iface.h 核心定义
#define pumpPWM_GPIO_RCU    RCU_GPIOA
#define pumpPWM_GPIO_PORT   GPIOA
#define pumpPWM_PIN         GPIO_PIN_8      // PA8 = TIMER0_CH0
#define pumpTIMER           TIMER0
#define pumpTIMER_RCU       RCU_TIMER0
#define pumpTIMER_CH        TIMER_CH_0
#define pumpPWM_MAX_VALUE   1000
#define pumpPWM_SET(x)      TIMER_CH0CV(pumpTIMER) = (uint32_t)(x)
```

**任务逻辑：**
```
- 水泵开关控制（全速 / 调速 / 停止 / 定时）
- 读取 IO_PUMP_CURR_AD 做过流保护
- 对外提供接口：bPump_SetLevel(u16 level)
```

---

### 任务 10 — **新增** `Hardware/O2Pump/` 氧气泵模块

**类型：新增**

**新建文件：**
- `Hardware/O2Pump/o2pump_iface.h`
- `Hardware/O2Pump/o2pump_task.h`
- `Hardware/O2Pump/o2pump_task.c`

```c
// o2pump_iface.h 核心定义
#define o2PWM_GPIO_RCU    RCU_GPIOC
#define o2PWM_GPIO_PORT   GPIOC
#define o2PWM_PIN         GPIO_PIN_7        // PC7 = TIMER2_CH1(全重映射)
#define o2TIMER           TIMER2
#define o2TIMER_RCU       RCU_TIMER2
#define o2TIMER_CH        TIMER_CH_1
#define o2PWM_MAX_VALUE   1000
#define o2PWM_SET(x)      TIMER_CH1CV(o2TIMER) = (uint32_t)(x)
```

**任务逻辑：**
```
- 氧气泵开关/调速控制
- 读取 IO_O2_CURR_AD 做过流保护
- 对外提供接口：bO2Pump_SetLevel(u16 level)
```

> ?? **注意**：O2泵(TIMER2_CH1)、加热棒(TIMER2_CH0)、FAN1(TIMER2_CH3)、FAN2(TIMER2_CH2) 共用 TIMER2，  
> TIMER2 的 Init 必须在 `md_hm_task.c` 中唯一调用，O2Pump 的 Init 只配置通道，不重复初始化定时器。

---

### 任务 11 — 12V使能控制 新增到系统初始化

**类型：新增（建议放在 `board_config.h` 或独立BSP文件）**

```c
// 12V电源使能，PC2，高电平有效
#define board12V_EN_RCU     RCU_GPIOC
#define board12V_EN_PORT    GPIOC
#define board12V_EN_PIN     GPIO_PIN_2
#define board12V_ON()       GPIO_BOP(board12V_EN_PORT) = board12V_EN_PIN
#define board12V_OFF()      GPIO_BC(board12V_EN_PORT) = board12V_EN_PIN
```

**调用时机：** 在 `sys_queue_task_init.c` 系统初始化完成后调用 `board12V_ON()`。

---

### 任务 12 — `Sys/sys_queue_task_init.c` 系统初始化任务更新

**类型：修改**

```
① 增加 O2Pump 模块初始化调用
② 增加 WaterPump 模块初始化调用
③ 增加 board12V_ON() 调用（开启12V总线）
④ 删除旧 DC/USB 电源模块初始化调用（或通过编译宏排除）
⑤ 确认 ADC 12路通道初始化正确
```

---

### 任务 13 — `Sys/sys_queue_task_work.c` 主工作任务更新

**类型：修改**

```
① 增加水泵工作状态机
② 增加氧气泵工作状态机
③ 更新加热棒控制：改用水温NTC1/NTC2 作为温度反馈源（取代旧NTC方案）
④ 更新灯光控制：改为 RGBW 四路独立控制接口
⑤ 增加光照传感器自动调光逻辑（boardLIGHT_SENSOR_EN）
```

---

### 任务 14 — 旧模块清理

**类型：清理**

```
① board_config.h：确认 boardDC_EN=0 / boardUSB_EN=0
② main.c 和 sys_task：通过编译宏排除 DC/USB 任务创建代码
③ Hardware/Dc/ 保留文件，但所有调用路径通过 boardDC_EN=0 编译排除
④ Hardware/Usb/ 同上处理
```

---

## 实施顺序

```
第一轮（能编译通过）：
  任务1 → 任务2 → 任务14 → 编译验证

第二轮（ADC正常工作）：
  任务3 → 任务4 → 编译 + ADC采样验证

第三轮（负载驱动）：
  任务5 → 任务6 → 任务7 → 任务8 → 任务9 → 任务10 → 任务11

第四轮（系统集成）：
  任务12 → 任务13 → 整体联调
```

---

## TIMER 资源汇总

| 定时器 | 通道 | 引脚 | 功能 | 备注 |
|--------|------|------|------|------|
| TIMER0 | CH0 | PA8 | 水泵PWM | 无重映射 |
| TIMER1 | CH2 | PB10 | 蜂鸣器PWM | 部分重映射 |
| TIMER2 | CH0 | PC6 | 加热棒PWM | 全重映射 |
| TIMER2 | CH1 | PC7 | 氧气泵PWM | 全重映射 |
| TIMER2 | CH2 | PC8 | 风扇2 PWM | 全重映射 |
| TIMER2 | CH3 | PC9 | 风扇1 PWM | 全重映射 |
| TIMER3 | CH0 | PB6 | 暖白光PWM | 无重映射 |
| TIMER3 | CH1 | PB7 | 蓝光PWM | 无重映射 |
| TIMER3 | CH2 | PB8 | 绿光PWM | 无重映射 |
| TIMER3 | CH3 | PB9 | 红光PWM | 无重映射 |

> ?? TIMER2 全重映射后 CH0~CH3 对应 PC6~PC9，Init 只能调用一次，各模块共享。
